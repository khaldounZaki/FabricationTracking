import 'package:flutter/foundation.dart';

class ReportController with ChangeNotifier {
  // Placeholder for future reporting queries
  // e.g., aggregate scan logs by user between two dates (from item/history collections)
}
